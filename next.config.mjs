/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['res.cloudinary.com'], // Add Cloudinary's domain for optimization
    },
    // You can add other Next.js configurations as needed
  };
  

export default nextConfig;
